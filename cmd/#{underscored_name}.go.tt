package main

import (
	"fmt"

	"github.com/<%=config[:author]%>/<%=config[:name]%>/pkg/api"
)

func main() {
	myVar, err := api.SayHello("pew pew")
	if err != nil {
		fmt.Println("Error: " + err.Error())
	}
	fmt.Println(myVar)
}
